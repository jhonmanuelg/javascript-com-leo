// Operacao aritmetica
const [a, b, c, d] = [3, 5, 1, 15]


const soma = a + b + c + d;
const subtracao = b - d;
const multiplicacao = a * b;
const divicao = d / a;
const modulo = a % 2;
console.groupCollapsed(soma, subtracao, multiplicacao, divicao, modulo)
